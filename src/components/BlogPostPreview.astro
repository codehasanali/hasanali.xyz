---
export interface Props {
	title: string;
	description: string;
	publishDate: string;
	url: string;
}

const { title, description, publishDate, url } = Astro.props as Props;
---

<article class="post-preview">
	<header>
		<button  class="back_btn"><a href="https://blog.hasanali.xyz">Back</a></button>
		<time>{publishDate}</time>
		<a href={url}><h1 class="title">{title}</h1></a>
	</header>
	<p>{description}</p>
	<a href={url}>Daha Fazlası</a>
</article>

<style>
	.back_btn {
	
		border: transparent;
		background-color: transparent;
		color:white;
		font-family: 'Courier New', Courier, monospace;
	font-size: 0.85em;
	}
	.post-preview {
		padding-bottom: 2rem;
		margin-bottom: 2rem;
		border-bottom: 4px solid var(--theme-divider);
	}

	.title,
	time {
		margin: 0;
	}

	time {
		font-size: 1.25rem;
		color: var(--theme-text-lighter);
	}

	.title {
		font-size: 2.25rem;
		font-weight: 700;
		color: var(--theme-text);
	}
</style>
